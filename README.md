To run locally, the backend and the frontend have to be run at the same time in separate terminals.

This project uses environment variables for API keys and database connections.

Create a .env file in the backend folder and copy the format from .env.example.
